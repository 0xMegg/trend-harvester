.DS_Store
*.log

# Harvest pipeline ephemeral data
harvest/.lock
harvest/.seen.json
harvest/raw/
harvest/analyzed/
harvest/rejected/

# Parallel epic worktree scratch (HARVEST_PARALLEL_WORKTREE=1)
.harvest-wt/

# Private MCP config (contains paths/secrets)
.mcp.json
